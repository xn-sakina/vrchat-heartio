import { Button } from '@mantine/core'

export default function App() {
  // const ipcHandle = (): void => window.electron.ipcRenderer.send('ping')

  return (
    <div>
      <Button fullWidth>111</Button>
    </div>
  )
}
